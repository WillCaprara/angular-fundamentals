import { Component } from "@angular/core";

@Component({
    selector: 'app-home', //To be a valid web component it actually needs a dash in the middle. Naming it only "home", as an element is technically incorrect.
    templateUrl: 'home.component.html'
})
export class HomeComponent {};